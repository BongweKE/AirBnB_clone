#!/usr/bin/env python3
"""a program called `console.py` that contains the entry point
of the command interpreter

***Your code should not be executed when imported
"""
import cmd

class HBNBCommand(cmd.Cmd):
    """Your command interpreter should implement:
    -quit and EOF to exit the program
    -help (this action is provided by default by cmd but you should
    keep it updated and documented as you work through tasks)
    -a custom prompt: (hbnb)
    -an empty line + ENTER shouldn’t execute anything

    """
    prompt = "(hbnb) "
    def do_quit(self):
        """Exit the console safely using quit prompt"""
        return True

    def do_EOF(self):
        """Exit the console safely using EOF (Ctrl+z or Ctrl+d) prompt"""
        return True

    def emptyline(self):
        """Edit what happens when enter is pressed on an empty line"""
        pass
